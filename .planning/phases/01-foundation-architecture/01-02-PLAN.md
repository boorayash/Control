---
phase: 01-foundation-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ARCH-03, UIPR-01]

must_haves:
  truths:
    - "React Viewer Web App can be built and served locally"
  artifacts:
    - path: "viewer/package.json"
      provides: "Vite React project configuration"
  key_links: []
---

<objective>
Setup React Viewer Client using Vite and Tailwind CSS

Purpose: Acts as the frontend for the user to view screens and send input controls
Output: Scaffolded Vite React application with Tailwind integrated
</objective>

<execution_context>
@C:/Users/yashb/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/yashb/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-architecture/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React app</name>
  <files>viewer/package.json, viewer/index.html, viewer/src/main.jsx, viewer/src/App.jsx</files>
  <action>Use the 'npm create vite@latest viewer -- --template react' command to scaffold the React app. Install 'socket.io-client'. Clean up the default boilerplate from App.jsx so it returns a clean, minimal div.</action>
  <verify>
    <automated>cd viewer && npm install && npm run build</automated>
  </verify>
  <done>Vite React app builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure Tailwind CSS and shadcn/ui</name>
  <files>viewer/tailwind.config.js, viewer/postcss.config.js, viewer/src/index.css, viewer/components.json</files>
  <action>Install tailwindcss, postcss, and autoprefixer inside the 'viewer' directory. Initialize tailwind configuration. Configure the content paths in tailwind.config.js to scan the src folder. Add the tailwind directives to src/index.css. Initialize shadcn/ui by running `npx shadcn@latest init -y` inside the viewer directory, applying default standard styles.</action>
  <verify>
    <automated>cd viewer && npm run build</automated>
  </verify>
  <done>Tailwind CSS is configured and the app builds successfully</done>
</task>

</tasks>

<verification>
Vite React app is properly generated with Tailwind CSS/shadcn configured and builds with no errors.
</verification>

<success_criteria>
1. viewer directory contains a Vite React app
2. Tailwind CSS and shadcn/ui components.json exist and are configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-architecture/01-02-SUMMARY.md`
</output>

---
phase: 03-remote-control-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [viewer/src/App.jsx]
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03]

must_haves:
  truths:
    - "Viewer captures mouse movements relative to the video element"
    - "Coordinates are normalized (0.0 - 1.0) before sending"
    - "Mouse moves are throttled to ~60fps"
  artifacts:
    - path: "viewer/src/App.jsx"
      provides: "Event listeners for mouse and keyboard"
---

<objective>
Implement Viewer input capture and Socket.IO emitting

Purpose: Capture mouse and keyboard events from the viewer UI and transmit them to the agent
Output: Viewer capable of sending control events over Socket.IO
</objective>

<execution_context>
@C:/Users/yashb/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/yashb/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remote-control-input/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement mouse and keyboard event listeners in Viewer</name>
  <files>viewer/src/App.jsx</files>
  <action>Add event listeners to the video element in `App.jsx`:
1.  **MouseMove**: Calculate coordinates relative to video element width/height, normalize them (0-1), and emit `mouse-move` via Socket.IO. Throttle using a 16ms timer or similar to achieve ~60fps.
2.  **MouseDown/MouseUp/Click**: Emit normalized coordinates + button type.
3.  **KeyDown/KeyUp**: Capture key codes and emit `key-down` / `key-up` events.
Ensure listeners only trigger when the video element has focus or the user is interacting with it.</action>
  <verify>
    <automated>cd viewer && npm run build</automated>
  </verify>
  <done>Viewer successfully captures and emits normalized input events</done>
</task>

</tasks>

<verification>
Viewer builds successfully and contains input event emitting logic.
</verification>

<success_criteria>
1. Viewer code contains `addEventListener` for `mousemove`, `mousedown`, and `keydown`.
2. Coordinates are divided by element dimensions before emitting.
3. Socket.IO emits 'mouse-move', 'mouse-click', and 'key-press' (or similar) events.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remote-control-input/03-01-SUMMARY.md`
</output>

---
phase: 03-remote-control-input
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [agent/package.json, agent/main.js, agent/index.html]
autonomous: false
requirements: [CTRL-04]

must_haves:
  truths:
    - "Host Agent executes input events natively"
    - "Normalized coordinates are scaled to host screen resolution"
  artifacts:
    - path: "agent/main.js"
      provides: "Native input execution logic"
---

<objective>
Implement Host Native OS input execution

Purpose: Receive input events from the viewer and execute them on the host system using robotjs
Output: Agent capable of controlling the host OS based on viewer input
</objective>

<execution_context>
@C:/Users/yashb/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/yashb/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remote-control-input/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install robotjs or nut-js in Agent</name>
  <files>agent/package.json</files>
  <action>Install `robotjs` in the agent directory. If `robotjs` fails to install due to build tool missing, fallback to `@nut-tree/nut-js`. Update `package.json` accordingly.</action>
  <verify>
    <automated>cd agent && (npm install robotjs || npm install @nut-tree/nut-js)</automated>
  </verify>
  <done>Input execution library is installed</done>
</task>

<task type="auto">
  <name>Task 2: Implement input execution in Agent</name>
  <files>agent/main.js, agent/index.html</files>
  <action>
1.  In `agent/index.html`, listen for Socket.IO events (`mouse-move`, `mousedown`, `keydown`, etc.).
2.  Send these events via IPC to the main process (`agent/main.js`).
3.  In `agent/main.js`, use `robotjs` to execute the commands:
    -   Scaling: Get screen size using `robot.getScreenSize()`. Multiply normalized coordinates by screen width/height.
    -   Mouse: `robot.moveMouse(x, y)`, `robot.mouseClick()`, etc.
    -   Keyboard: `robot.keyTap(key)`, etc.
</action>
  <verify>
    <automated>node --check agent/main.js</automated>
  </verify>
  <done>Agent successfully executes native input commands</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-End Remote Control</what-built>
  <how-to-verify>
    1. Start signaling server, viewer, and agent.
    2. Click and move mouse in viewer.
    3. Observe host machine cursor mirroring movements.
    4. Type in viewer and observe input on host.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Agent builds successfully and contains native input execution logic.
</verification>

<success_criteria>
1. `agent/main.js` imports the input library.
2. IPC listener handles input events.
3. Coordinates are scaled by actual screen resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remote-control-input/03-02-SUMMARY.md`
</output>
